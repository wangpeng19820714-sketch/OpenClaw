# Lobster Release

Custom OpenClaw extension workspace for a Godot release center.

Current state:

- `docs/` stores the product and technical design notes.
- Runtime code is intentionally not started yet; build the service after the API and data model are agreed.
